{#
/**
 * @file dd_organization.html.twig
 * Default theme implementation to present DD Organization data.
 *
 * This template is used when viewing DD Organization pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_dd_organization()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('dd_organization') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
